---
deployment:
  tasks:
    - export DEPLOYPATH=/home4/singathi/public_html/wp-content/themes/singathiwe-media/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -av --delete --exclude='.git' --exclude='.github' --exclude='.cpanel.yml' ./ $DEPLOYPATH
